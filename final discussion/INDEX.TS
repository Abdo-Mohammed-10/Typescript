//homepage
let el =document.getElementById("id") as HTMLElement;
type settings={
    theme: boolean;
    font:string;
    sidebar:boolean;
    external:boolean;

}

let usersettings:settings={
    theme:true,
    font:"open sans",
    sidebar:false,
    external:true
}